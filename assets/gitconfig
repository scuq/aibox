# Generated by aibox — the container's entire git identity. There is no user
# name, no email, no credential helper: commits are made by the user on the
# host, always. See `ainotes`.
[safe]
	directory = /work
[gc]
	auto = 0
[core]
	fsmonitor = false
[credential]
	helper =
